#ifndef HEX_TESTS_TRAMPOLINE_H
#define HEX_TESTS_TRAMPOLINE_H

/*
 * Moved to sw/common/tests/ztramp.h when sw/apps/sheet's render needed
 * the same thing -- which is the condition the original file's own
 * note said to promote it on. Kept here as a shim so this app's
 * render.c, and the build lines quoted in its comments, still work
 * unchanged.
 */

#include "../../../common/tests/ztramp.h"

#endif
