// included by blinkh.v: one bit wider than blink.v's counter, so the LED
// blinks at half blink.bit's rate -- a check, on the board, that the
// hierarchy, the parameter and this `define all took
`define BLINK_BITS 25
